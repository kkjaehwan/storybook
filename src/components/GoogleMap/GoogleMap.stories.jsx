import React from "react";
import GoogleMap from "./GoogleMap";

export default {
  component: GoogleMap,
  title: "Design System/GoogleMap/GoogleMap",
  tags: ["autodocs"],
};

const Template = (args) => <GoogleMap {...args} />;

export const Default = Template.bind({});
Default.args = {

};
